# lesson6

Класс `Matrix` с перегрузкой операторов: `+`, `-`, `*`, `+=`, `-=`, `*=`, `<<`, `>>`.

## Сборка

```bash
mkdir build
cd build
cmake ..
cmake --build . --config Debug
```

Запуск: `Debug\matrix.exe`
